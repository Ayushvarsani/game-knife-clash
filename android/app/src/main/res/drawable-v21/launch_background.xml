<?xml version="1.0" encoding="utf-8"?>
<!-- Native launch screen: theme background, no logo. The logo lives only in the
     Flutter splash. Color matches Flutter splash so there is no flash on load. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
</layer-list>
